# V5 ImagesWorker 무료 드라이런

이미지 API와 LLM API를 호출하지 않았다. ScriptWorker의 scene_type 분류와 ImagesWorker가 사용하는 V5 계약 생성만 실행했다.

## kospi_july_01

- scene_type: `graph`
- archetype: `data_lab`
- provider / tier: `gemini_pro` / `hero`
- primary surface: one central holographic map panel
- deterministic verified overlay: `apply_verified_values_after_generation` (input present: `True`)
- selection reason: 일반 시계열·시장 그래프이므로 지도 경로·계기면·콘솔이 있는 data_lab을 추천

## kospi_july_02

- scene_type: `graph`
- archetype: `trade_calculator`
- provider / tier: `gemini_pro` / `hero`
- primary surface: the broad engraved front plinth directly beneath the scale's central pillar
- deterministic verified overlay: `apply_verified_values_after_generation` (input present: `True`)
- selection reason: 비교·막대·비율 표현이므로 저울·문서·벽면 다이어그램이 있는 trade_calculator를 추천

## kospi_july_03

- scene_type: `metric`
- archetype: `risk_control_room`
- provider / tier: `gemini_pro` / `hero`
- primary surface: the single large central analog gauge dial face embedded at eye level in the curved operations wall
- deterministic verified overlay: `apply_verified_values_after_generation` (input present: `True`)
- selection reason: 하락·위험·변동 지표이므로 계기판·경고 명판·콘솔이 있는 risk_control_room을 추천

## general_port_narrative_04

- scene_type: `general`
- archetype: `port_emergency`
- provider / tier: `gemini_pro` / `body`
- primary surface: the broad painted front side of the single largest foreground shipping container nearest the center dock
- deterministic verified overlay: `not_applicable` (input present: `False`)
- selection reason: 항만·물류 장소 단서가 있어 캐릭터 중심의 port_emergency를 추천

## text_key_message_05

- scene_type: `text`
- archetype: `classroom`
- provider / tier: `gemini_pro` / `hero`
- primary surface: the broad central chalk-writing area of the large green teaching wall
- deterministic verified overlay: `apply_verified_values_after_generation` (input present: `False`)
- selection reason: 짧은 정의·핵심 문구는 분필 판서와 고정 메모가 있는 classroom을 추천
