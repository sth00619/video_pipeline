# 8월 코스피 스타일 계약 4장면 파일럿 — 검수 dossier

검수일: 2026-08-02  
생성 상태: **4장 PNG 저장 완료**  
재호출 상태: 없음. 최초 실행의 로컬 시간 제한 뒤 `--resume`으로 파일만 복구했으며 Gemini API를 다시 호출하지 않았다.

## 비용 및 원장 상태

- 사전 추정: ₩1,016 (4장 × ₩254)
- 요청 원장: 3건은 `HTTP 200`, 마지막 항만 씬은 PNG가 저장됐지만 로컬 제한 시간 때문에 원장 완료 상태가 기록되기 전 종료됐다.
- 따라서 비용 상태는 **콘솔 대조 전 미확정**이다. 다음 유료 단계 전 Gemini 콘솔에서 4건을 대조해야 한다.

## 공통 계약 검수

| 기준 | 결과 |
|---|---|
| 굵은 검정 윤곽선과 셀 셰이딩 | 충족 |
| 갈색 페도라, 네이비 슈트, 큰 눈, 뺨 홍조 | 충족 |
| 수치·사실 수치·Pillow 오버레이 없음 | 충족 |
| 워터마크·한국어 텍스트 없음 | 충족 |
| 참조 이미지 2장 전달 | 충족 (request ledger 기록) |

## 장면별 판정

| 씬 | 문구/소품 | 판정 | 근거 및 후속 |
|---|---|---|---|
| `kospi_august_semiconductor` | `SEMICONDUCTOR GOES DOWN` / 방송 스튜디오의 대형 벽면 | PASS | 문구가 벽면 그래프와 결합돼 있고, 명확한 하락선·페도라·두꺼운 윤곽선이 모두 보인다. |
| `kospi_august_dollar` | `DOLLAR UNDER PRESSURE` / 방송 스튜디오의 대형 벽면 | PASS (다양성 주의) | 문구·하락 화살표·캐릭터 계약은 충족한다. 다만 첫 씬과 같은 `data_lab` 배경이므로 최종 영상에서는 다른 archetype으로 바꾸는 편이 좋다. |
| `kospi_august_fear` | `FEAR IS COOLING` / 중앙 아날로그 게이지 | REGENERATE | 주 문구는 게이지에 자연스럽게 결합됐지만, 아래 보조 콘솔에도 `COOLING`이 중복되어 "하나의 정보 소품 표면" 계약을 엄격히 위반한다. 재생성 시 보조 콘솔의 모든 글자를 금지한다. |
| `general_port_narrative_04` | 문구 없음 / 폭풍 항만 | PASS | 항만 단일 archetype, 빗속 조명, 컨테이너·선박 디테일, 페도라 위 안전모와 경보 행동이 목표 화풍에 맞는다. |

## OCR 상태

호스트의 파일은 FastAPI 컨테이너에 마운트되지 않아 Tesseract OCR을 실행할 수 없었다. 이 결과는 OCR 통과로 처리하지 않으며, 위 텍스트 판정은 이미지 직접 검수 기준이다.

## 최종 판정

**조건부 통과 (3 PASS / 1 REGENERATE).**

다음 유료 작업은 자동 실행하지 않는다. 승인 시 다음 중 하나를 선택해 진행한다.

1. `kospi_august_fear`만 재생성하여 보조 콘솔의 중복 영문을 제거한다.
2. `kospi_august_dollar`도 함께 재생성하고 `weather_map` 또는 `trade_calculator`로 바꿔 배경 다양성을 높인다.
