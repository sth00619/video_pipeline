# Overlay grammar visual fixtures

The regression tests render the deterministic overlay grammar at source size
and at 320 px mobile width.  Fixtures stay code-generated because their
readable Korean copy, exact figures, and layout geometry are part of the
contract rather than model-generated bitmap text.
