# V5 primary surface 장식 문자 재검증 — 01번

작성일: 2026-07-31

## 실행 범위

- run ID: `kospi_july_2026_scene_type_primary_text_v3`
- 장면: `kospi_july_01` (`graph` → `data_lab`)
- primary physical surface: `one central holographic map panel`
- 자동 재시도: 없음
- 예상 단가: `$0.14` / `₩254`
- 원장 상태: `http_200`, `unverified_until_console_reconciliation`

## 육안 결과

- primary 지도 패널 내부에는 `GLOBAL DATAFLOW`, `MARKET NODE A`, `MARKET NODE B`, `SAMPLE 45.2%`가 읽을 수 있는 장식 표기로 생성됐다.
- 표기는 지도 패널의 광원·원근·테두리 안에 들어가 있어 별도 카드나 화면 위 UI처럼 떠 있지 않다.
- primary 이외의 콘솔·사이드 소품에는 읽을 수 있는 문자·숫자가 보이지 않고 색 블록·막대 실루엣·추상 선만 남아 있다.
- 로고·한글 자막·워터마크는 보이지 않는다.

## 판정 범위

이 결과는 “primary에는 표기를 의무화하고, 그 외 표면은 비문자로 유지”하는 프롬프트 균형이 `data_lab`에서 작동함을 보인다. `trade_calculator`와 `risk_control_room`은 아직 호출하지 않았으므로 이 문서만으로 3씬 전체 통과를 선언하지 않는다.
