# 전역 작업 규칙

- LLM 모델은 `claude-sonnet-4-6`으로 고정하며 어떤 이유로도 변경하지 않는다.
- API 키(`ANTHROPIC_API_KEY`, `YOUTUBE_API_KEY` 등), JWT secret, DB 비밀번호를 하드코딩하지 않는다. 필수 환경 변수가 없으면 시작 단계에서 실패 처리한다.
- LLM은 지수값·등락률 등 수치형 금융 데이터를 생성하지 않는다. 수치 오버레이는 Pillow와 FFmpeg로 결정론적으로 렌더링한다.
- 영상 1건의 총비용은 20분 미만: ₩40,000, 20분 이상: ₩70,000로 제한한다. 비용 관련 상수는 `PricingConfig.java`에서 중앙 관리한다.
- 코드 주석과 UI 텍스트는 한국어로 작성한다.
- 사실 확인이 필요한 외부 API 사양은 추측하지 않는다. 검증할 수 없으면 그 사실을 명시적으로 플래그한다.

## 파이프라인 보완 우선 원칙

- 새 요구사항은 기존 단계의 대체가 아니라, 필요한 계약과 선택 경로를 추가하는 방식으로 구현한다.
- 대본, TTS, 이미지, 조립 단계 중 하나를 바꾸기 전에는 직전·직후 단계의 입력, 해시, 산출물 계보를 확인하고 무관한 기능의 동작을 변경하지 않는다.
- 승인 대본의 내레이션 원문은 TTS 입력과 영상 자막의 단일 기준이다. 표시를 위한 줄바꿈 외의 문자·숫자·단위 변경은 허용하지 않는다.
- 기사형·상황형·정보형 같은 시각 모드는 동일한 승인 대본 장면에 선택적으로 결합하며, 기존 일반형 이미지와 캐릭터·V5·archetype 계약을 폐기하거나 전역적으로 덮어쓰지 않는다.
- 현재 승인 범위를 벗어난 E2E 단계는 실행하지 않는다. 특히 TTS/자막 검증 중에는 Kling, MP4 조립, 썸네일 생성을 시작하지 않는다.

## 공통 운영 반영 강제 원칙

- Job 번호와 scene 번호는 문제를 재현하는 fixture일 뿐이다. 제품 코드에 특정 Job·scene·주제만 고치는 분기, 하드코딩, 프롬프트 예외를 추가하지 않는다.
- 문제를 고치기 전에 먼저 입력 데이터·과거 산출물·공급자 일시 오류 같은 개별 실행 문제인지, 모든 Job에 재발할 수 있는 공통 계약 문제인지 증거로 분류한다. 원인 범위를 확인하지 않은 채 전역 정책을 바꾸거나 개별 패치를 만들지 않는다.
- 테스트와 실제 파일럿에서 채택된 최종 결론을 영상 생성 버튼이 타는 가장 이른 공통 경계에 반영한다. 최초 가설이나 중간 우회책은 운영 정책으로 남기지 않으며, 폐기한 전략과 폐기 이유를 개발 문서에 기록한다.
- 공통 반영 범위에는 AUTO/GUIDED/MANUAL, 신규 Job/과거 Job 재개, 병렬/순차 처리, 전체 생성/단일 장면 재생성이 모두 포함된다. 어느 한 경로가 장면 메타데이터·계보·품질 게이트를 우회하지 않는지 확인한다.
- 테스트·진단 스크립트·파일럿 래퍼에만 구현된 기능은 완료로 보지 않는다. UI → Spring → FastAPI worker → 실제 공급자 요청 → 품질 게이트 → DB 자산/manifest → 다음 단계 소비 경로를 추적해 운영 연결을 검증한다.
- 선행 실패 테스트 → 최소 공통 수정 → 집중 회귀 → 전체 회귀 → 위험에 비례한 실제 API/E2E 검증 순서를 지킨다. 품질 게이트를 낮추거나 실패 시 조용히 다른 렌더러로 대체해 통과시키지 않는다.
- 각 단계 산출물은 적용한 공통 계약 버전, 입력 해시, 주요 통과·보류 판정을 보존해야 한다. 새 주제의 Job에서도 같은 계약이 실제 실행됐는지 DB 자산과 manifest로 사후 확인할 수 있어야 한다.
- 모든 변경의 목적은 특정 예시를 복제하는 것이 아니라, 서로 다른 수많은 주제와 대본에서 KEYWORD/RESEARCH → SCRIPT → TTS → IMAGES → LONGFORM 5단계 영상 제작 품질을 높이는 것이다.
- 유료 이미지 canary 결과는 실물 이미지를 사용자에게 첨부하고, 사용자 육안 확인 전에는 `개선됨`, `승인 근접`, `통과`로 판정하지 않는다. 자동 얼굴·OCR·물리 표면 게이트는 그대로 유지하며, 별도로 캐릭터 해부학, 채널 화풍, 장면 의미 전달, 예상 밖 시각 결함을 점검해 manifest에 보류 또는 사용자 판정을 남긴다.
- 이미지의 “일관성”은 모든 장면의 표정·의상·배경·문구를 같게 만드는 뜻이 아니다. 캐릭터 정체성·해부학·화풍·문자 정확성·장면 의미 같은 공통 합격 하한선은 모든 장면에 동일하게 적용하고, 표정·의상·행동·배경은 승인 대본과 SceneDirector가 정한 장면별 변수로 검증한다. 한 축만 실패했으면 통과한 축을 보존하는 표적 재생성으로 고치며, 특정 장면의 연출을 전역 템플릿으로 승격하지 않는다.
